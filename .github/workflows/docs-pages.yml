name: Docs Pages (Disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Explain disabled workflow
        run: |
          echo "GitHub Pages deployment is disabled in this repository."
          echo "Docs publishing is now expected to run from a central docs repository."
